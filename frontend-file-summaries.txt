{
  "frontend/src/pages/JobBoard.tsx": "Public job board page; `backendFilter.jobType` now correctly resolves to `JobType | undefined` since `FilterState.jobType` is `JobType | ''`, eliminating the type assignment error.",
  "frontend/src/pages/JobDetailPage.tsx": "Job detail page showing full job info and application form; JOB_TYPE_LABELS and JOB_TYPE_COLORS maps updated to use backend enum keys with 'nysc' displaying as 'NYSC (Youth Corps)'.",
  "frontend/src/pages/JobPostingForm.tsx": "Job posting/editing form for posters; `form.jobType` is now typed as `JobType` using the imported enum, with `JobType.fullTime` as default and proper enum casts on select change and existing job load.",
  "frontend/src/pages/JobApplicantsView.tsx": "Poster-facing page listing all applicants for a specific job with seeker profile info, skills, cover message, accept/reject actions, and a rating form for accepted applicants.",
  "frontend/src/pages/AdminDashboard.tsx": "Admin-only dashboard with analytics, paginated job/seeker/poster tables; added JOB_TYPE_LABELS map so job type column displays human-readable labels including 'NYSC (Youth Corps)' for 'nysc' type jobs.",
  "frontend/src/components/Layout.tsx": "App shell with sticky header, mobile nav, and footer; all logo image references now point to `/assets/openj1.png` with alt text \"Open-Jobs\", replacing the previous open-jobs-logo asset.",
  "frontend/src/components/LandingPage.tsx": "Public landing page with hero, stats, features, how-it-works, and CTA sections; job type badges now show 'NYSC (Youth Corps)' instead of 'Short-Time', and the hero description text is updated accordingly.",
  "frontend/index.html": "HTML entry point with the browser tab title updated to \"Open-Jobs\".",
  "frontend/src/components/JobFilters.tsx": "Filter sidebar with keyword, location, job type select, skills, and pay range; `FilterState.jobType` is now typed as `JobType | ''` and select item values use the `JobType` enum constants, ensuring type-safe filter state.",
  "frontend/src/components/JobListingCard.tsx": "Reusable job listing card component for browse/poster/seeker modes; JOB_TYPE_LABELS and JOB_TYPE_COLORS maps updated to use backend enum keys (camelCase) with 'nysc' mapping to 'NYSC (Youth Corps)'.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interactions; `useCreateJobListing` now types `jobType` as `JobType` (imported from backend) instead of `string`, fixing the type mismatch at the actor call site."
}